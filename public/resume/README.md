# Resume PDF

Place the approved public resume here (for example `chase-norvell-resume.pdf`),
then set `resumePath` in `src/config/site.ts` to:

```ts
resumePath: "/resume/chase-norvell-resume.pdf",
```
